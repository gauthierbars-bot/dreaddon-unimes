create table schema_unimes.test_table as unimes (
    PersonID int,
    LastName varchar(255),
    FirstName varchar(255),
    Address varchar(255),
    City varchar(255)
)
select * from unimes;
