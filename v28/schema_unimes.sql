CREATE SCHEMA IF NOT EXISTS schema_unimes;

CREATE TABLE schema_unimes.test_table (
    PersonID int,
    LastName varchar(255),
    FirstName varchar(255),
    Address varchar(255),
    City varchar(255)
);